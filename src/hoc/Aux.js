import React from 'react';

const aux = props => props.children;

export default aux;

/// this is an emtpy wrapper that uses the children property, and children refers to the content in the tag it is used in.
/// and outputs that content